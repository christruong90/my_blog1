<h1> All Comments </h1>

<% @comment.each do |comment|  %>
  <div>
    <!-- <h2><%= link_to comment.body, comment_path(comment) %></h2> -->
    <p><%= link_to comment.body, comment_path(comment) %></p>
    <hr>
  </div>
<% end %>
